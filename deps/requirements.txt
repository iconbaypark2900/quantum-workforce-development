# Full local development, CI, and Docker/HF images; Vercel installs from deps/requirements-vercel.txt (see vercel.json installCommand).
#
# Core scientific computing
numpy==2.3.3
pandas==2.3.3
scipy==1.16.2
scikit-learn==1.7.2

# Graph/networking
networkx==3.5

# Visualization
matplotlib==3.10.6
seaborn==0.13.2

# PDF report generation (requires system libs: libpango, libcairo — see deploy/docker/Dockerfile.fly)
weasyprint>=62.3

# Web framework
flask==3.1.2
flask-cors==6.0.2
flask-limiter==4.1.1
flask-jwt-extended>=4.6.0

# Data/API
requests>=2.31.0          # Required by TiingoProvider (HTTP REST)
yfinance==0.2.66          # Legacy fallback — deprecated; use Tiingo instead

# Alternative data providers (optional)
# alpaca-py>=0.10.0  # Alpaca Markets (free tier)
# polygon-api-client>=1.12.0  # Polygon.io (paid)

# Testing
pytest==8.4.2
pytest-cov==7.0.0

# Monitoring and logging
python-json-logger==3.3.0
prometheus-client==0.23.1
psutil>=5.9.0

# Utilities
tqdm==4.67.1
pyyaml==6.0.3
python-dotenv==1.1.1
cryptography>=42.0.0

# Optional: HMM-based regime detection
hmmlearn>=0.3.3

# AWS Braket (optional: for quantum annealing backend)
amazon-braket-sdk>=1.60.0
amazon-braket-schemas>=1.18.0
boto3>=1.28.0

# IBM Quantum (optional: VQE hardware + GET /api/config/ibm-quantum/workloads)
# Reproducible install: pip install -r deps/requirements-ibm-quantum.txt
# qiskit==2.3.1
# qiskit-ibm-runtime==0.46.1
